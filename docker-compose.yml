version: "3.8"

services:
  db:
    image: postgres:13.1-alpine
    env_file: .env
    restart: always
    ports:
      - "5432:5432"
    volumes:
      - db-data:/var/lib/postgresql/data/

volumes:
  db-data:
